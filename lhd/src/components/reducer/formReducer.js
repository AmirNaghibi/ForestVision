import extend from 'extend';

const initialState = {
    toggleModal: false,
}

export default function(state = initialState, action){
    switch(action.type){

        default:
            return state;
    }
}